<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hunger Help</title>
    <link
      href="https://fonts.googleapis.com/css?family=McLaren|Montserrat&display=swap"
      rel="stylesheet"
    />
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <!-- bootstrap -->
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <!-- <h1 id="togglemode">Hunger Help</h1> -->
        <div id="buttons">
            <a class="nav-link-1 active nav-link-btns" href="/">Home</a>
            <a class="nav-link-2 active nav-link-btns" href="/donate-food">Donate Food</a>
            <a class="nav-link-3 active nav-link-btns" href="/request-food">Request Food</a>
            <a class="nav-link-4 active nav-link-btns" href="/statistics">β-Bots</a>
            <a class="nav-link-5 active nav-link-btns" href="/register">Join</a>
            <a class="nav-link-6 active nav-link-btns" href="/about">About</a>
        </div>
    </header>
    <div id="shutter">
        <div id="shutter-circle"></div>
        <div id="shutter-rod"></div>
    </div>
    <script src="https://unpkg.com/@dotlottie/player-component@latest/dist/dotlottie-player.mjs" type="module"></script> 
</body>
</html>
<script>
    // When the page loads, retrieve the state from local storage and apply it to your elements.
window.addEventListener("load", () => {
    const header = document.querySelector("header");
    const shutterRod = document.getElementById("shutter-rod");
    const navLinks = document.getElementsByClassName("nav-link-btns");

    // Retrieve the state from local storage
    const isHeaderActive = localStorage.getItem("isHeaderActive") === "true";

    // Apply the state to your elements
    if (isHeaderActive) {
        header.classList.add("active");
        shutterRod.classList.add("active");
        for (let i = 0; i < navLinks.length; i++) {
            navLinks[i].classList.remove("active");
        }
    }
    else{
        for (let i = 0; i < navLinks.length; i++) {
            navLinks[i].classList.add("active");
        }
    }

    // Add an event listener to your button to toggle the state and store it in local storage
    shutterRod.addEventListener("click", () => {
        header.classList.toggle("active");
        shutterRod.classList.toggle("active");
        for (let i = 0; i < navLinks.length; i++) {
            navLinks[i].classList.toggle("active");
        }

        // Store the state in local storage
        const isHeaderActive = header.classList.contains("active");
        localStorage.setItem("isHeaderActive", isHeaderActive);
    });
});
</script>